generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "sqlite"
  url = env("DATABASE_URL")
}
model Play {
  id String @id @default(cuid())
  boardgameName String
  bggId String
  playersJson String
}
